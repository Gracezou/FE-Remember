module.exports = {
    semi: false, //是否在行尾加分号
    singleQuote: true, //单引号
    useTabs: false, //使用tab（制表符）缩进而非空格
    tabWidth: 4,
    trailingComma: 'none', //数组、对象最后一个元素的尾逗号
    bracketSpacing: true, //花括号前后空格
    printWidth: 180, //每行到多少长度开始折行
    jsxBracketSameLine: true, //使多行JSX元素最后一行末尾的 > 单独一行
    arrowParens: 'avoid' //只有一个参数的箭头函数的参数是否带圆括号
}
